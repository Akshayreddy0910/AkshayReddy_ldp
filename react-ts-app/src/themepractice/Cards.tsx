import { Card, CardContent, Typography } from "@mui/material";

const Cards = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">React</Typography>
        <Typography>Material UI Card Example</Typography>
      </CardContent>
    </Card>
  );
};

export default Cards;
