const Story = () => {
  return (
    <section className="view">
      <h1>Our Story</h1>
      <p>Chapter demonstrates client-side routing with React Router v6 — route config, nested layouts via Outlet, and NavLink-based navigation.</p>
    </section>
  )
}

export default Story
